import Home from "../components/Home";

export default function Page() {
  return (
    // We just render the Home component here.
    // In Next.js, pages are server components by default.
    <Home />
  );
}
